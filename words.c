#include <stdio.h>
#include <string.h>
 
void main()
{
    char n[200];
    int count = 0, i;
 
    printf("enter the string\n");
    scanf("%s", n);
    for (i = 0;s[i] != '\0';i++)
    {
        if (s[i] == ' ')
           count++;    
    }
    printf("number of words in a string are: %d\n", count +1);
}
