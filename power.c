#include<stdio.h>
#include<conio.h>
int main()
{
float base,exp,sum;
printf("enter the base number");
scanf(%f",&base);
printf("enter the exponent");
scanf("%f",&exp);
sum=pow(base,exp);
printf("%f the sum is",sum);
return 0;
}

