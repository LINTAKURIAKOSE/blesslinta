#include <stdio.h>
  int main()
  {
    int a[10];
    int i;
    int g;
    printf("Enter ten values:");
	scanf("%d",&a[i]);
    for (i = 0; i < 10; i++)
     g = a[0];
    for (i = 0; i < 10; i++) 
        (a[i] > g)
	{
     printf(" lagest of ten numbers is %d",g);
    return 0;
  }
}
