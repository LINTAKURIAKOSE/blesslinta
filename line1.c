#include<stdio.h>
#include<string.h>
void main()


{ 
    char para[30],i,line=0,n; 
     printf("Enter your paragraph\n\n\t: ");
     scanf("%d",&n);
    for(i=0;i<=1;i++)
    if(para[i]='.')
    {
        line++;
    }
    printf("number of sentence%d",line);
}
